name: Breath

on:
  push:
  schedule:
    # Runs "at every start of month" (see https://crontab.guru)
    - cron: "0 0 1 * *"
jobs:
  generate-source:
    name: Generate Source And Update Repo
    permissions: write-all
    runs-on: ubuntu-latest
    #if: github.event_name != 'push'
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Generate Source Codes
        run: |
          sudo apt-get update
          sudo apt-get install -y wget apt-transport-https software-properties-common
          wget -q "https://packages.microsoft.com/config/ubuntu/$(lsb_release -rs)/packages-microsoft-prod.deb"
          sudo dpkg -i packages-microsoft-prod.deb
          sudo apt-get update
          sudo apt-get install -y powershell
          pwsh -noprofile -executionpolicy bypass -file language-colors.ps1
          cp ./vanillajs/language-colors.css ./docs/
          cp ./nodejs/language-colors.js ./docs/
          ls -l ./docs/

      # should push to main branch

  run-tests:
    name: Generate Source And Update Repo
    permissions: write-all
    needs: generate-source
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Run NodeJS Test
        run: |
          cd nodejs
          npm install
          npm test

      # should push to main branch

  deployment:
    name: Deploy To Package Registries
    permissions: write-all
    runs-on: ubuntu-latest
    needs: [generate-source, run-tests]
    #if: github.event_name != 'push'
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Changelog
        uses: scottbrenner/generate-changelog-action@master
        id: Changelog
        env:
          REPO: ${{ github.repository }}

      # create github release
      # - uses: "marvinpinto/action-automatic-releases@latest"
      #   with:
      #     repo_token: "${{ secrets.GITHUB_TOKEN }}"
      #     automatic_release_tag: "latest"
      #     prerelease: false
      #     title: "v2.${{ github.run_attempt }}.${{ github.run_number }}"
      #     files: |
      #       "css/language-colors.css"
      #       "vanillajs/language-colors.js"
      #       "language-colors.ps1"
      #       "nodejs/language-colors-2.${{ github.run_attempt }}.${{ github.run_number }}.tgz"

      - name: Build Node Package
        run: |
          sudo apt-get update
          sudo apt-get install -y nodejs npm
          npm install -g json
          json -I -f package.json -e 'this.version="v2.${{ github.run_attempt }}.${{ github.run_number }}"';
          cat package.json
          npm pack
          ls -l

      # create npm release
      # - uses: "marvinpinto/action-automatic-releases@latest"
      #   with:
      #     repo_token: "${{ secrets.GITHUB_TOKEN }}"
      #     automatic_release_tag: "latest"
      #     prerelease: false
      #     title: "v2.${{ github.run_attempt }}.${{ github.run_number }}"
      #     files: |
      #       language-colors.css
      #       language-colors.js
      #       language-colors.ps1
